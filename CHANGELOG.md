# Changelog

## [Unreleased]

### Added

- **Карточка пользователя в админке пересобрана в панель оператора.** Слева —
  колонка личности с аватаркой из Telegram и действия, сгруппированные по
  риску: продление и копирование ссылки отдельно, удаление — в обведённой
  «опасной зоне» внизу. Справа — строка состояния («осталось 30 дней ·
  израсходовано 0,3% трафика · устройства заняты 2 из 2»), четыре плитки
  параметров с подсказкой «Изменить» внутри рамки, платежи и рефералы над
  сгибом и системная строка. На телефоне та же лента и липкая нижняя панель с
  продлением; отдельного мобильного дерева разметки больше нет.

- **Аватарка пользователя из Telegram в карточке админки.** Берётся тем же
  механизмом, что и в профиле кабинета: подписанная ссылка на
  `/cabinet/api/avatar`, срок жизни — сутки. Наличие фотографии заранее не
  проверяется (это стоило бы запроса в Telegram на каждую карточку) — если её
  нет или она закрыта приватностью, рисуются инициалы. У web-клиентов без
  Telegram ссылка не выдаётся.

- **Декор-темы кабинета сами включаются по праздникам.** В админке появилась
  подсекция «Оформление → Авто-смена по праздникам»: список окон «с ДД.ММ по
  ДД.ММ», у каждого своя тема. Окно живёт без года и повторяется ежегодно, может
  идти через 31 декабря (Новый год — с 1 декабря по 31 января) и не обязано
  совпадать с самим праздником: Хэллоуин по умолчанию включается за неделю до и
  держится неделю после.

  Внутри окна его тема перекрывает выбранную вручную, после окна возвращается
  прежняя. Пересеклись два окна — побеждает верхнее, порядок двигается стрелками;
  ненужное окно выключается тумблером, не удаляясь. По галочке без всякой
  настройки работает встроенный пресет (Новый год, 14 февраля, Хэллоуин, чёрная
  пятница, весна, лето). Настройки: `CABINET_DECOR_AUTO_ENABLED` и
  `CABINET_DECOR_SCHEDULE` в [documentation/env.md](documentation/env.md).

- **Heleket — новая касса «Другая крипта».** Счёт выставляется сразу в рублях, доступна
  и в кабинете, и в боте. Оплату подтверждает вебхук, но статус всегда перепроверяется
  запросом к самой кассе, а поллер добирает случаи, когда вебхук не дошёл; просроченные
  счета закрываются сами. Чеки «Мой налог» по этой кассе не отправляются намеренно.
  Настройка: `HELEKET_*` в [documentation/env.md](documentation/env.md), описание —
  [documentation/payments.md](documentation/payments.md).

- **«Обзор» в админке стал дашбордом.** Раньше на `/admin` лежали плитки-ссылки на
  разделы — меню внутри бокового меню. Теперь это оперативный экран: онлайн сейчас,
  трафик за сегодня, состав пользователей панели, деньги за день и месяц.

  Главное на нём — блок **«требует внимания»**: просроченная и подходящая оплата
  серверов, заявки партнёров, заявки на выплату и колесо фортуны, ушедшее в минус.
  Красным помечено то, что стоит денег или гасит сервер, оранжевым — важное, но не
  срочное; когда дел нет, плашка зелёная. Каждая строка ведёт туда, где дело чинится.

  Данные панели (трафик, онлайн, узлы, память) кэшируются на 30 секунд. Если панель
  не отвечает, страница не падает: числа магазина остаются на месте, а вместо
  панельных стоит честная плашка.

- **Статистика разбита на четыре вкладки** — «Обзор», «Деньги», «Пользователи»,
  «Механики» — и получила метрики, которых не было: воронку «регистрация → счёт →
  оплата», разрез «когда покупают» по дням недели и часам, средний срок жизни
  подписки, продления против первых покупок, разбивку по кассам за выбранный период
  и сводку партнёрской программы.

  Проценты роста теперь считаются для **любого** периода, включая произвольный
  диапазон: сравнивается текущее окно с предыдущим такой же длины. Раньше сравнение
  существовало только для календарного месяца.

  Шага «зашёл на сайт» в воронке нет: визиты нигде не учитываются, и рисовать этот
  шаг «на глаз» значило бы выдумать конверсию.

- **Колонки в таблицах статистики тянутся мышью и пальцем**, ширины запоминаются.
  Подобрать их «на всех» нельзя: у кого-то коды промокодов из двух символов, у
  кого-то из пятнадцати.

- **Уведомления партнёрской программы в Telegram.** Админу — о новой заявке в партнёры
  и о запросе на вывод; партнёру — решение по его заявке и по его выводу. Ваша половина
  по умолчанию идёт в личку, отдельный чат или тема форума задаются
  `PARTNER_NOTIFY_CHAT_ID` и `PARTNER_NOTIFY_MESSAGE_THREAD_ID`.

  Уведомлений о каждом начислении и о переходах по ссылке **нет намеренно**: отключить
  их партнёр не может, а на работающей программе это превращается в спам. Баланс и
  статистику он смотрит в кабинете, когда захочет.

  Отправка не влияет на результат операции: заявка создана и вывод обработан
  независимо от того, дошло ли сообщение. Партнёру, зарегистрированному только через
  веб-кабинет, не пишем — Telegram-аккаунта у него нет.

- **Партнёрская программа.** Партнёр приводит клиентов и получает процент деньгами с
  каждой их оплаты: по умолчанию 40% с первой и 20% со всех последующих, бессрочно.
  Не путать с реферальной — та платит днями подписки и обеим сторонам. Клиент попадает
  ровно в одну программу, и это правило зашито в SQL, а не в код: закрепление
  отбивается, если человек уже платил или уже пришёл по реферальной ссылке.

  Партнёром становятся по заявке с модерацией или по ручному назначению админом.
  У партнёра баланс с холдом (по умолчанию 7 дней), потоки-ссылки под разные площадки
  со своей статистикой, реквизиты и заявки на вывод; у админа — очередь заявок,
  индивидуальные проценты поверх глобальных, ручные корректировки баланса, заморозка
  и отзыв статуса, обработка выплат с номером чека.

  Деньги считаются как журнал: балансы денормализованы и меняются только в той же
  транзакции, что и запись начисления, повторная обработка платежа отбивается
  уникальным индексом по покупке, а сходимость проверяется отдельно
  (`CheckBalances` и интеграционный тест на полный цикл). Автовыплат нет — вывод
  обрабатывает человек.

  Партнёр **не видит контакты своих клиентов**: ники, `@username`, почты и telegram id
  маскируются. В админке маски нет — иначе спор не разобрать.

  Оплаты в Telegram Stars учитываются по `RUB_PER_STAR`; если курс не задан,
  начисление пропускается с предупреждением и счётчиком в админке — процент от
  количества звёзд как от рублей дал бы случайную сумму.

  Десять настроек, все редактируются из админки без перезапуска.
  Миграция `000044`. Документация: [documentation/partner-program.md](documentation/partner-program.md).

### Fixed

- **На «Тарифах» съезжала шапка.** Сетка карточек центрируется своим `max-width`
  внутри более широкой страницы, а заголовок с кнопкой «назад» шёл во всю ширину —
  и стоял левее карточек. Теперь заголовок и сетка держат одну колонку. Заодно
  подзаголовок встал под заголовок, а не под кнопку «назад»: это же чинит шапки
  «Настроек» и «Лояльности».

- **У красной кнопки не было отклика на наведение.** `destructive` осветлялся через
  `brightness`, а на тёмной теме такое осветление красного почти не видно. Теперь
  у неё свой цвет наведения и красное свечение — как у главной кнопки.

- **Топ рефереров считал не то.** В колонке «оплативших» стояло число **всех**
  приглашённых, а из таблицы выпадали активные рефереры, которые сами ничего не
  покупали. Теперь оплатившие считаются среди приглашённых, добавились «принесли
  деньгами» и «начислено дней». То же число уходит в Telegram-бота — там подпись
  была так же неверной.

- **«Всего трафика» в «Обзоре» показывало сырое число** вида `132114059095594`
  вместо `120,2 TiB`.

- **Дробные числа и проценты печатались через точку** в русском интерфейсе:
  «4.8 мес» и «36.5% базы» вместо «4,8 мес» и «36,5%».

### Changed

- **Карточка пользователя доведена до макета.** Строка состояния убрана —
  те же цифры стоят в плитках ниже. Списки платежей и рефералов свёрнуты до
  трёх строк с кнопкой «Раскрыть список»: карточка с сорока платежами больше
  не растягивает страницу и не ломает симметрию колонок. Радиус контролов
  админки приведён к макету: карточка 14px вместо 20px, кнопка и поле — 10px.
  Опасная зона, системная строка и меню действий (в нём снова все пункты, а не
  три) собраны один в один с макетом; в окне продления календарь занимает всю
  ширину. На телефоне карточка живёт без общего хедера: сверху «назад», номер
  и меню, в шапке — кнопка «поделиться», снизу — липкая панель продления. В
  списке пользователей у колонок появились значки.

- **Хвост хлебных крошек снова доезжает.** Страницы вызывали
  `useAdminPageMeta` выше `AdminLayout`, то есть над провайдером контекста, и
  хук всегда получал null: вместо «@username» в крошках стоял запасной
  «Пользователь #id». Мета передаётся пропом.

- **Модалки карточки пользователя приведены к одной системе.** Три ширины
  (`sm` / `md` / `lg`) вместо произвольных, один футер: слева необратимое
  действие или счётчик, справа «Отмена» и главная кнопка. Окно срока действия
  получило «+7 / +30 / +90 / +365 дней» и открывается на текущей дате, а
  календарь ужат вдвое. В трафике сверху видно, сколько уже израсходовано, и
  добавлен пресет «Безлимит». В устройствах лимиты собраны в одну строку, а
  отвязка подтверждается прямо в строке вместо системного `window.confirm`.
  Удаление пользователя перечисляет последствия и предлагает отключение как
  безопасную замену.

- **Deep link подключения и кнопки бота уехали из «Оформления кабинета»** в
  отдельную группу «Подключение и Telegram» — соседнюю, в той же вкладке
  «Оформление». Шифрование ссылок и кнопки в minimalism-меню бота к оформлению
  отношения не имели, а с появлением расписания праздничных тем секция стала
  слишком длинной. Значения настроек не менялись, переезжать ничего не нужно.

- **Истории оплат и лояльности стали компактнее.** Дата сократилась до `28.08.26`,
  время ушло второй строкой мелким и приглушённым. В истории оплат колонка «Способ»
  исчезла: название метода дублировало тип покупки по весу, поэтому осталось значком
  перед суммой — теми же карточкой, кошельком, биткоином и Telegram, что и в выборе
  метода на чекауте. Суммы выровнены по правому краю моноширинными цифрами. На
  телефоне обе таблицы превращаются в строки-карточки: слева тип и дата, справа
  сумма или начисление. В истории лояльности колонка «Платёж» стала «Источником» —
  значок и подпись, сумма деталью под ней, — а XP теперь бейдж. Обе истории листаются
  одинаково, по 20 записей на страницу: раньше оплаты грузились разом сотней, а
  лояльность листалась по десять.

- **Модалки подтверждения кабинета сведены к одной.** Выход из аккаунта, удаление
  устройства и отвязка способа входа были тремя разными диалогами; у последнего текст
  вообще был вшит в код мимо переводов. Теперь это общий компонент: появление с
  анимацией, значок, пояснение, что именно произойдёт, закрытие по Esc и клику по фону.
  Подтверждение стоит слева, отмена справа.

- **Страница реферальной программы переписана как оффер.** Калькулятор бонусных
  дней с ползунком «сколько друзей приглашу за год», график накопленных дней,
  схема потока и карточки приглашения. График, схема и плитки результата стали
  общими с партнёрской страницей: программы считают разное — проценты и дни, — а
  раскладка у них одна, и две копии разъехались бы первой же правкой.

- **Страница партнёрской программы переписана как оффер.** Тот, кто ещё не подал
  заявку, видит калькулятор дохода: ползунок «клиентов в месяц» пересчитывает
  проценты в рубли, а график накопленного дохода показывает то, чего из двух цифр
  не видно, — процент с продлений повторяется, поэтому заработок копится вместе с
  базой приведённых клиентов. Линия рисуется слева направо и повторяется по кругу.
  Ниже — схема потока «аудитория → ссылка → оплата → баланс» и витрина кабинета
  партнёра с примером статистики по потокам.

  Форма заявки уехала в модальное окно по кнопке «Стать партнёром»: раньше она
  стояла третьим блоком и человек видел её раньше, чем успевал понять условия.

  На десктопе оффер занимает широкую колонку; дашборд партнёра и экран «заявка на
  рассмотрении» остались узкими, как остальной кабинет. Оценка дохода строится по
  ценам публичной витрины тарифов и снабжена подписью о допущениях.

- **Реферальные начисления теперь записываются, а не пересчитываются.** Появился
  журнал `referral_bonus_ledger` (миграция `000043`): строка добавляется после
  успешного продления в панели, вместе со ставками, из которых сложились дни.

  До этого «заработано дней» каждый экран восстанавливал сам, пересчитывая историю
  покупок по текущим настройкам — четырьмя независимыми реализациями одной формулы.
  У такого пересчёта два неустранимых порока: он врёт после любой правки настроек
  (админ поднял ставку — и в истории задним числом «выросли» начисления, которых
  никто не получал) и ломается на помесячном начислении, где восстановить прошлое
  из счётчика покупок уже нельзя.

  При обновлении история восстанавливается разовым бэкфиллом — по плоской формуле,
  как реально начислялось до появления помесячного режима, чтобы цифры не подскочили
  в день апдейта. Миграция аддитивная, откат на предыдущую версию безопасен.

- **Поддержка Remnawave `3.4.*`.** Диапазон совместимости расширен с `3.0.0`–`3.3.*` до
  `3.0.0`–`3.4.*`. Изменений в коде бота не потребовалось.

  Единственная ломающая правка API в `3.4.0` — в объекте хоста: `excludedInternalSquads`
  заменён на `internalSquads` с полями `mode` и `squads`. Бот не обращается к `/api/hosts`
  ни из Go, ни из кабинета, поэтому правка его не задевает. Теги, добавленные профилям,
  шаблонам, плагинам и сквадам, приходят новыми полями в ответах; клиент панели декодирует
  их обычным `json.Unmarshal`, незнакомые поля игнорируются — `/api/internal-squads`
  читается по-прежнему.

  Проверено не рассуждением, а прогоном: стенд обновлён `3.3.2` → `3.4.0`, контрактный
  набор прошёл целиком. Подтверждено на живой `3.4.0`: миграция хостов даёт
  `{"mode":"EXCLUDE","squads":[]}` и доступ не сужает; `username` в PATCH `/api/users`
  всё ещё игнорируется (merge кабинета опирается на `remnawave_user_id`); панель всё ещё
  допускает дубли `telegramId` (порядок удаления в merge обязателен). Детали —
  [documentation/compatibility.md](documentation/compatibility.md).

- **Мигратор Bedolaga: цель исправлена с Remnawave `2.8.*` на `3.3.*`–`3.4.*`.**
  Wizard `scripts/meows-bedolaga-migrate.sh` требовал подтвердить, что панель — ветка
  `2.8.*`, и **прерывал миграцию** при честном ответе «нет», хотя бот `5.x` работает
  только с `3.x`. Движок (`tools/migrate-bedolaga`) при этом давно ходит через общий
  `remnawave.Client`, то есть был 3.x-нативным — устарели только гейт и тексты.
  Заодно в `sourceCaps.WarningLines()` была инвертирована логика подсказок: связка
  Bedolaga `3.x` + RW `2.x` называлась «good match», а `4.x` + RW `3.0+` — проблемой.
  Теперь наоборот, как и должно быть для бота `5.x`.

- **Кнопка «Отправить приглашение» убрана из модалки подключения устройства.**
  Осталось копирование приглашения и QR-код.

  Кнопка вызывала Web Share API, и он недостижим там, где живёт основная часть
  пользователей: `navigator.share` не реализован в Android WebView, а Telegram
  открывает мини-апп именно в нём. На десктопе шит есть, но показывает только
  приложения, зарегистрированные как share target, — Telegram туда не входит, и
  список оказывался пустым. Работал сценарий только в мобильном браузере.

  До этого видимость кнопки чинилась дважды: сначала `pointer: coarse` (описывает
  точность указателя, а не платформу), затем определение платформы через
  `navigator.userAgentData.mobile` / UA — и всё равно кнопка не появлялась в
  мини-аппе, потому что условие видимости дополнительно требовало
  `navigator.share`. Держать правило видимости ради одного мобильного браузера
  смысла нет: копирование работает всюду одинаково.

  Если понадобится «поделиться» именно внутри Telegram, правильный путь —
  не `navigator.share`, а WebApp API:
  `openTelegramLink('https://t.me/share/url?url=…&text=…')` открывает родной
  выбор чата. Причина решения записана комментарием в `ConnectExtraDevice.tsx`.

- **Длинные второстепенные секции телеграм-поста о релизе сворачиваются в
  раскрывающуюся цитату** (`<blockquote expandable>`): `Улучшения`,
  `Исправления`, `При обновлении`, `Технические изменения`, `Breaking changes` —
  начиная с трёх пунктов. `Новое` не сворачивается никогда. В `RELEASE_NOTES.md`
  разметка не пишется, её добавляет `tools/release` (`collapsibleSection`).
  На лимит 4096 символов это не влияет — Telegram считает символы текста.

## [5.2.0] - 2026-08-28

### Added

- **Подключение доп. устройства по ссылке/QR без входа в кабинет** (`/connect`, `cabinet_connect_invite`).
  Ссылка живёт 3 дня, получатель подключается сам. Заменила карточку «ссылка подписки»
  на странице подписки.

- **График расхода трафика на главной** — от начала расчётного периода подписки,
  появляется от двух дней истории.

- **Флоу нового пользователя**: предложение пробного крупной цифрой, а при
  недоступном пробном — цена «от», лимит устройств и выгода за год со ссылкой на тарифы.
  После активации — подсказка, куда нажать для подключения.

- **Редизайн главной и страницы подписки** (кольца дни/трафик/устройства,
  единая карточка подписки, быстрые переходы по разделам).

### Fixed

- **Merge аккаунтов кабинета пересобран.** Аудит нашёл девять дефектов, каждый
  воспроизведён интеграционным тестом против живого PostgreSQL до правки.
  Полное описание инвариантов и карта кода — `.cursor/docs/reference/account-linking-and-merge.md`,
  раздел «Реализация».

  - **Выбранная подписка могла остаться ссылкой на удалённый профиль панели.**
    При `keep=web` с привязанным Telegram выживала tg-строка `customer`, забирала
    `expire_at`/`subscription_link` с web-стороны, но `loserForRemnawave` был жёстко
    прописан как web-сторона — merge удалял в Remnawave ровно тот профиль, ссылку
    на который только что записал победителю. Разведены две независимые оси:
    какая СТРОКА выживает (`survivingCustomerRow`) и чья ПОДПИСКА выживает (`keep`).

  - **Merge не знал про миграцию `000041`.** `remnawave_user_id` / `remnawave_short_uuid`
    не читались и не переносились, из-за чего выживший клиент указывал на профиль
    проигравшей стороны. Теперь колонки читаются в `lockCustomer*`, переносятся
    в транзакции и подтверждаются после синка панели.

  - **Синтетический `telegram_id` попадал в `cabinet_identity(telegram)`.**
    При слиянии двух web-аккаунтов создавалась фиктивная telegram-привязка:
    `/me` показывал «Telegram подключён» с несуществующим id, кнопка привязки
    исчезала, и настоящий Telegram привязать было уже нельзя. Введён
    `realTelegramID`, синтетика отсекается в `SaveEmailPeerClaim`,
    `telegramIDFromIdentity` и `ensureCabinetTelegramIdentityTx`.

  - **Второй кабинет-аккаунт не поглощался при привязке Telegram через OIDC.**
    В `linkTelegramIdentity` ветка «просто telegram-claim» стояла раньше проверки
    владельца, поэтому `PeerAccountID` в claim не попадал: после merge два аккаунта
    оставались на одном `customer`, либо второй терял `link` вместе с подпиской,
    а его telegram-привязку забирал текущий. Порядок проверок исправлен; вдобавок
    merge сам собирает владельцев участвующих `customer` в `sides.absorb`.

  - **Ветки noop/linked выходили до поглощения peer.** Email/пароль и соц-привязки
    второго аккаунта молча терялись, а merge рапортовал успех. Все формы merge
    сведены в один хвост транзакции.

  - **`link` обновлялся мимо транзакции и вхолостую.** `s.links.UpdateCustomerID`
    работал через пул, а при отсутствии строки затрагивал 0 строк — аккаунт
    оставался без клиента при ответе `linked`. Заменено на `upsertAccountLink`
    внутри `tx`.

  - **Каскад уносил данные проигравшей строки.** `promo_redemption` (одноразовый
    промокод можно было активировать заново), `customer_pending_discount`,
    `fortune_spins`, дедуп `customer_lifecycle_notify_sent` гибли вместе с
    `DELETE FROM customer`. `movePerCustomerRecords` переносит их с учётом
    уникальных ключей.

  - **`is_web_only = FALSE` ставился безусловно** — даже когда у выжившего остался
    синтетический `telegram_id`. Теперь строго: web-only ⟺ нет настоящего Telegram.

  - **Поглощение затирало email и пароль выжившего** чужими. Теперь свой парольный
    логин сохраняется, а email-identity поглощаемого удаляется, а не переносится
    дублем (её `provider_user_id` — id удалённого аккаунта).

- **Кнопка «Поделиться» в модалке подключения устройства пряталась на мобильных.**
  Видимость решалась медиазапросом `pointer: coarse`, но он описывает точность
  указателя, а не платформу, и промахивается в обе стороны: ноутбук с тачскрином
  отдаёт coarse (системный шит там пустой, ради чего кнопку и скрывали), а
  мобильный WebView с подключённой мышью или в desktop-режиме отдаёт fine.
  Платформа определяется напрямую: `navigator.userAgentData.mobile`, иначе UA,
  отдельной веткой iPadOS Safari (представляется Macintosh, отличается тачем).
  Регрессия жила только внутри невыпущенной 5.2.0 — в релизноты не выносилась.

- **Раскрывашка «Настроить вручную» прилипала к нижнему краю модалки.**
  Это последний элемент листа, и одного padding контейнера текстовой строке
  не хватало (`pb-2`).

### Changed

- **Выбор подписки при merge спрашивается только когда обе подписки активны**
  (`expire_at > now`). Раньше достаточно было непустого `expire_at` у обеих
  сторон, поэтому выбор предлагался и между живой подпиской и истёкшей год назад —
  с риском случайно выбрать мёртвую. Если живая одна, она побеждает автоматически;
  если обе истекли — берётся та, что истекла позже. Дни проигравшей стороны
  по-прежнему сгорают, суммирования нет.

- **`/me` отдаёт тот `telegram_id`, по которому клиента адресует бот.** После
  слияния двух аккаунтов с настоящими Telegram на выжившем остаются обе привязки
  (вход работает через любую), и раньше в ответ попадала первая попавшаяся.

- Удалён мёртвый код merge: `detectDanger`, `mergedSubscriptionLink`, `maxExpireAt`,
  `activeSubscriptionConflict`, `ErrDangerousConflict`. Поля ответа
  `is_dangerous` / `danger_reason` оставлены всегда пустыми ради совместимости
  JSON-контракта с выкаченным фронтендом.

### Notes

- **Панель Remnawave 3.3.2 не даёт переименовать профиль:** `username` в
  `PATCH /api/users` молча игнорируется (200, значение прежнее). Переименование
  профиля под нового владельца из merge убрано; имя остаётся от прежнего владельца,
  поэтому поиск по префиксу `<customerID>_` для слитых клиентов неверен —
  надёжна только привязка `remnawave_user_id`. См. TD-017.
- **Панель допускает два профиля с одним `telegramId`**, поэтому профиль
  проигравшей стороны удаляется строго ДО патча победителя.
- Новые тесты: 18 интеграционных сценариев merge (`-tags=integration`, нужен
  одноразовый PostgreSQL) и контрактные вызовы панели (`-tags=contract`).
  В CI не участвуют — оба набора за build-тегами.

## [5.1.1] - 2026-08-26

### Fixed

- **Рывок заголовков при открытии кабинета: шрифты переехали с Google Fonts в `public/fonts`.**
  Inter и Manrope тянулись через `@import` в первой строке `index.css`, то есть браузер узнавал
  о них только после загрузки и разбора бандла стилей: HTML → `index-*.css` →
  `fonts.googleapis.com` → `fonts.gstatic.com`. До конца этой цепочки заголовки рисовались
  системным шрифтом и потом подменялись. До 5.1.0 подмену маскировал `animate-fade-in` на
  контейнере контента в `AppLayout` — каждая страница проявлялась 0.3 с при монтировании;
  вместе с переходом на `PageReveal`, который играет только при первом заходе, маскировка
  исчезла и дефект стал виден на всех страницах.

  16 начертаний (Inter 400/500/600/700, Manrope 700/800, JetBrains Mono 400/500; latin +
  cyrillic, 417 КБ) отдаются со своего домена, `unicode-range` сохранён — страница по-прежнему
  качает только нужные подмножества. Inter 400 и Manrope 700 прогреваются через `preload`
  в `index.html`. Трафика не прибавилось: те же файлы шли с gstatic, но на два round-trip
  позже и с внешней зависимостью, ненадёжной в РФ. Хендлер SPA отдаёт `fonts/` с длинным
  кэшем, иначе повторный заход снова показывал бы подмену. Генерация — `scripts/fetch-fonts.py`.

- **Мигание заголовков в Telegram Mini App: «лесенка» там больше не играет.** Анимация
  `opacity` + `transform` на крупных текстовых блоках давала микро-пропадание заголовков,
  причём границы мигания совпадали ровно с `RevealItem`, а в обычном мобильном браузере
  того же эффекта не было. Похоже на компоновку слоёв: блок промотируется в композитный слой
  на время анимации и обратно, и WebView теряет его содержимое на кадр. Механизм без устройства
  подтвердить не удалось, поэтому анимация отключена там, где ломается, а не подобрана по
  параметрам. Признак сессии Telegram фиксируется при загрузке модуля — react-router переписывает
  hash при первой навигации, и `tgWebAppData=` пропадает. Скелетоны и заглушки работают везде.

## [5.1.0] - 2026-08-26

### Added

- **Появление блоков кабинета «лесенкой» при первом заходе на страницу.** Приём тот же, что на
  лендинге (`features/landing/components/LandingMotion.tsx`), но с двумя отличиями: оркестрация
  через variants — шаг задаётся один раз на контейнере `PageReveal`, а не руками в каждом блоке;
  и анимация играет только при первом заходе на страницу за загрузку приложения. Возврат на уже
  виденную страницу рисуется сразу, иначе навигация туда-обратно превращалась бы в мигание.
  При `prefers-reduced-motion` всё рендерится статично.

- **Скелетоны загрузки на всех страницах кабинета** (`components/ui/skeleton.tsx`). Заглушки держат
  высоту будущего блока, поэтому подстановка данных не двигает вёрстку. Вместо пульсации opacity —
  проход блика: на тёмной теме пульсация читается как мигание. Существующие ad-hoc заглушки
  (`SkeletonRows` на подписке, `animate-pulse` на тарифах) переведены на общий примитив,
  текстовые `common.loading` заменены на скелетоны соответствующей раскладки.

### Fixed

- **Главная показывала чужие данные до ответа сервера.** Пока запрос подписки не завершился,
  `hasSubscription` равнялся `false`, и рисовалась карточка триала с `trial?.days ?? 0` — на
  медленной сети пользователь видел «0 дней / 0 ГБ / 0 устройств» и «Пробный период недоступен»
  при активной подписке, а через секунду содержимое подменялось. Карточка ждёт ответа под
  скелетоном. Запрос устройств включается только после подписки, поэтому его состояние читается
  через `isLoading`, а не `isPending` — иначе заглушка висела бы вечно.

- **Мигание шапки при скролле в мобильном мини-аппе.** Шапка — sticky-элемент в потоке документа,
  а её строка пряталась через `display:none`. Каждое переключение меняло высоту документа на ~56px:
  браузер пересчитывал `scrollY`, приходил новый scroll-эвент, обработчик возвращал шапку, высота
  росла обратно — самоподдерживающийся цикл на частоте rAF. В Telegram Mini App заводился сам, ещё
  до касания экрана: анимация `requestFullscreen` меняет `--tg-safe-area-inset-top`, контент
  дашборда догружается асинхронно — оба дают «фальшивые» дельты скролла. Пороги в 4/6px были ниже
  уровня шума webview с инерцией и rubber-band. Авто-скрытие убрано и в кабинете, и в админском
  хуке (та же схема с `max-md:hidden` внутри sticky-шапки). Вернуть фичу можно только
  layout-нейтрально: fixed-шапка с распоркой и transform.

- **Мигание заголовков при первом заходе в мини-аппе.** Допуск в 500 мс на повторное монтирование
  ставился ради `React.StrictMode`, но действовал и в проде: первый заход в мини-аппе идёт через
  дополнительный цикл авторизации по `initData`, страница успевала перемонтироваться в это окно,
  и «лесенка» проигрывалась второй раз — уже показанный блок уходил в `opacity: 0` и проявлялся
  снова. Теперь допуск живёт только в dev-сборке.

### Changed

- **`tg.disableVerticalSwipes()` при инициализации мини-аппа** (Bot API 7.7+). У кабинета
  собственный вертикальный скролл, а жест «свайп вниз = свернуть» перехватывал часть касаний.
  Закрытие по ✕ работает как раньше.

- **Убран шрифт Onest.** Грузился из `index.html` в первой волне переменным начертанием 300..900
  с кириллицей, но не использовался нигде: ни в коде, ни в `tailwind.config.ts` — единственным
  упоминанием во всём репозитории была сама ссылка. При этом отбирал полосу у Inter и Manrope,
  которыми набран весь кабинет.

## [5.0.1] - 2026-08-25

### Fixed

- **Не сохранялись доп. слоты устройств в админке кабинета.** Модалка устройств копит правки
  и отправляет их одним запросом с суммарной разницей: выставить 7 доп. слотов означало
  `delta=7`, а эндпоинт принимал только `±1` и отвечал `400 delta must be 1 or -1`.
  Ошибка не воспроизводилась, если доп. слоты не менялись (например, при базовом лимите 10),
  поэтому выглядела произвольной.

  Ограничение было артефактом валидации, а не логики: обработчик складывает дельту с текущим
  значением и сам зажимает результат по нулю снизу и по `HWID_MAX_DEVICES` сверху. Теперь
  принимается любая ненулевая дельта в разумных пределах — правка остаётся **одним атомарным
  запросом** вместо серии из семи, где сбой на середине оставил бы лимит в промежуточном
  состоянии.

## [5.0.0] - 2026-08-25

> **Ломающее обновление.** Поддержка Remnawave `3.0.0`–`3.4.*`. Совместимость с панелью `2.8`
> прекращена: панель и бот обновляются вместе. Ветка `4.x` заморожена для тех, кто остаётся на `2.8`.
> Порядок перехода и откат — [documentation/updating.md](documentation/updating.md).

### Changed

- **Идентификатор пользователя панели: `uuid` → числовой `id`.** В Remnawave 3.0.0 поле `uuid`
  удалено из объекта пользователя. Переведены `remnawave.User`, `UpdateUserRequest`, все пути
  (`/api/users/{userId}`, `/api/hwid/devices/{userId}`, `/actions/reset-traffic`, `/actions/extend`)
  и тело `POST /api/hwid/devices/delete` (`userUuid` → `userId`). UUID сохранили сквады
  (`activeInternalSquads`, `externalSquadUuid`) — конфигурация сквадов в `.env` не меняется.
- **Поиск клиента: `GET /api/users/by-telegram-id/{id}` заменён на `GET /api/users/stream`.**
  Точечные lookup-эндпоинты (`by-telegram-id`, `by-email`, `by-tag`, `by-id`) удалены в 3.0.0.
  Пагинация курсорная; учтена асимметрия спеки — в запросе `cursor` числовой, в ответе
  `nextCursor` строковый. На stream переведён и полный обход `GetUsers`, чтобы пагинация
  жила в одном месте.
- **Продление подписки считает панель** — `POST /api/users/{userId}/actions/extend` вместо
  чтения `expireAt` с последующим `PATCH`. Убирает гонку read-modify-write, когда бот и кабинет
  продлевают одного клиента одновременно. Семантика дат совпадает с прежней: активной подписке
  дни добавляются к `expireAt`, истёкшей — считаются от текущего момента.
- **Обработка ответов без тела.** В 3.0.0 `DELETE` отвечает `204`, фоновые операции — `202`,
  оба без тела; создание ресурсов — `201`. Декодирование ответа пропускается централизованно,
  иначе успешный запрос падал с `unexpected end of JSON input`.

### Added

- **`customer.remnawave_user_id` и `customer.remnawave_short_uuid`** (миграция `000041`).
  Раньше профиль панели искался заново при каждой операции, а на fallback-путях это был полный
  обход списка пользователей. Заполняются лениво при первом обращении к клиенту — отдельный
  скрипт не нужен, и схема самовосстанавливается: неверная запись чинится следующим резолвом.
  Миграция строго аддитивная, поэтому откат бота не требует восстановления БД.
- **`YOOKASA_PROXY_URL`** — прокси только для запросов к API ЮKassa (`http`, `https`, `socks5`,
  можно с логином и паролем в URL). Нужен, когда сеть до `api.yookassa.ru` нестабильна или
  блокируется: часть TLS-соединений отваливается по таймауту, и оплата падает с `500`.
  Глобальный `HTTPS_PROXY` для этого не подходит — он увёл бы в прокси и обращения к панели
  по внутреннему docker-DNS. Док: [documentation/payments.md](documentation/payments.md).
- **Логин web-клиента в веб-админке.** До первой покупки у такого клиента нет ни `@username`,
  ни профиля в панели — в списке он выглядел как `#222`, и найти его было нечем. Теперь в списке
  и в карточке показывается логин вида `222_meows` (`<id>_<local-part email>` — ровно тот,
  под которым профиль создаётся в Remnawave), а поиск в админке дополнительно ищет по email
  аккаунта кабинета. Правило формирования логина вынесено в `utils.PanelLoginForCustomer`
  и используется и выдачей подписки, и админкой — чтобы показываемое совпадало с тем,
  что реально уходит в панель.
- **Контрактные тесты против живой панели** (`internal/remnawave/contract_test.go`, build-тег
  `contract`). Проверяют то, что не ловят юнит-тесты: реальные пути, JSON-теги, ответы без тела,
  курсорную пагинацию и оживление заблокированного клиента при продлении.

### Fixed

- **Продление не снимало блокировку с клиента.** Панельный `extend` переводит в `ACTIVE` только
  статус `EXPIRED`, а `DISABLED` и `LIMITED` оставляет как есть. Клиент с превышенным трафиком
  или заблокированный админом оплатил бы продление и остался без доступа. Статус добивается
  отдельным `PATCH` — только когда он не `ACTIVE`, и без `expireAt`, чтобы не вернуть гонку по дате.
- **`DeleteInfraBillingNode` падал на успешном удалении** — `DELETE /api/infra-billing/nodes/{uuid}`
  в 3.x отвечает `204` без тела, а код пытался разобрать ответ как JSON.
- **Админские операции падали с 400 у заблокированных клиентов.** Админка читает карточку
  пользователя и отправляет её обратно вместе с правкой — включая текущий статус. В 3.x панель
  отдаёт `ACTIVE`, `DISABLED`, `LIMITED` или `EXPIRED`, но на запись принимает только первые два:
  на `LIMITED`/`EXPIRED` возвращается `400 Validation failed`. То есть смена сквадов, лимитов,
  тега или календаря ломалась ровно у тех клиентов, которых админ правит чаще всего —
  с превышенным трафиком или истёкшей подпиской. Статус нормализуется в `PatchUser`,
  то есть централизованно для всех вызывающих. Невалидное значение вычищается, а не подменяется
  на `ACTIVE`: правка тега не должна снимать блокировку.
- **Продление могло начислить дни дважды.** Продление стало двумя запросами (расчёт даты панелью
  и добивающий `PATCH` статуса). Если падал второй, ошибка уходила наверх — вызывающий не сохранял
  новый `expire_at` и мог повторить операцию, а дни уже были начислены. Теперь сбой реактивации
  только логируется: двойное начисление необратимо, а неснятый статус чинится следующей операцией.
- **Пустой ответ панели молча превращался в пустую подписку.** Пропуск декодирования при
  безтелесном ответе применялся и там, где тело ожидалось, — вызывающий получал нулевую
  структуру и записывал в БД пустой `subscription_link` и `expire_at = 0001-01-01`.
  Теперь отсутствие тела там, где оно нужно, — явная ошибка.
- **Фильтр `stream` по `telegramId` не перепроверялся.** Раньше отбор гарантировал сам
  эндпоинт `by-telegram-id`; теперь это query-параметр, и если панель его проигнорирует,
  вернутся все пользователи, а выбор «первого подходящего» отдаст чужой профиль —
  продление, смена лимита и удаление ушли бы на посторонний аккаунт. Результат фильтруется
  повторно на стороне бота.
- **Сохранённый `remnawave_user_id` принимался на веру.** Ошибочная привязка становилась
  вечной: быстрый путь всегда успешен, поиск её никогда не перезаписывал, и клиент видел бы
  чужие устройства и подписку. Владение профилем проверяется по `telegram_id`.
- **Пагинация могла зациклиться.** Если панель перестанет двигать курсор и продолжит отдавать
  `hasMore=true`, обход копил бы страницы до исчерпания памяти. Добавлены потолок числа
  страниц и проверка, что курсор изменился.
- **Удаление ноды в infra-billing показывалось как ошибка.** Нода удалялась, но сбой
  перечитывания списка возвращал 500 — админ видел «не удалось», а повторная попытка
  упиралась в 404. Перечитывание больше не влияет на результат операции.
- **`YOOKASA_PROXY_URL` без схемы ломал все платежи.** `host:1080` разбирается без ошибки,
  но валит каждый запрос с `unsupported protocol scheme`, и заявленный откат на прямое
  соединение не срабатывал. Схема проверяется явно.
- **Веб-админка показывала неверный Telegram ID у web-клиентов.** Синтетические id
  (`CABINET_WEB_TELEGRAM_ID_BASE` + id аккаунта) превышают `Number.MAX_SAFE_INTEGER`,
  поэтому JSON-число теряло в браузере последние разряды: `7000000000000000108`
  превращалось в `7000000000000000000`. Различаются такие id как раз хвостом, так что
  в списке все web-клиенты выглядели одинаково. `telegram_id` в admin API сериализуется
  строкой (`json:",string"`), тип в SPA — `string`. Личный кабинет не затронут:
  `/me` синтетический id не отдаёт вовсе.
- **Скидочный промокод без TTL нельзя было применить.** Валидация требовала
  `discount_ttl_hours`, но форма позволяла сохранить промокод без него — активация падала
  с неинформативным `promo_validation_failed`. Пустое поле теперь означает «скидка без срока»,
  как и в соседних полях той же формы, где пусто = «безлимит». Отрицательное значение
  по-прежнему отклоняется.
- **Тема `nebula`: надпись на кнопке активации триала почти не читалась.** Утилиты кнопки
  (`bg-primary/10` + `text-primary`) рассчитаны на полупрозрачную заливку, а тема красит
  `.cabinet-btn-primary` градиентом через `background-image`, который `background-color`
  не перекрывает — голубой текст оказывался на голубом градиенте. Текст сделан белым,
  прозрачность неактивной кнопки поднята с `0.5` до `0.72`.
- **Клиент ЮKassa работал без таймаута** — зависший запрос удерживал HTTP-обработчик кабинета.
  Установлен лимит 30 секунд на всю операцию, включая TLS.

### Removed

- **Сопоставление по `remnawave uuid` в `tools/migrate-bedolaga`.** В панели 3.x этого поля больше
  нет. Записи Bedolaga, у которых нет ни `telegram_id`, ни `short_uuid`, попадут в отчёт
  как несопоставленные.

## [4.13.1] - 2026-08-24

### Changed

- **Тема `nebula`:** пятно света под курсором ослаблено вдвое (`0.16` → `0.08`, радиус `260` → `240px`) — в кабинете оно перетягивало внимание с содержимого карточки. Кольцо по контуру и тень не менялись.
- **Тема `nebula`:** активный пункт в шапке подсвечивается градиентной линией вместо серой пилюли (`bg-secondary`). Пилюля меняла ширину при раскрытии подписи на hover и подталкивала соседние иконки. Вариант с пилюлей остался под `data-nebula-nav="pill"` (ставит только dev-превью).

## [4.13.0] - 2026-08-24

### Added

- **Публичный лендинг** (`web/cabinet/src/features/landing/`): hero, витрина тарифов, три шага подключения, FAQ, финальный CTA, футер. Отдаётся с двух адресов — `/landing` (корень домена, `mux.Handle` в `internal/cabinet/http/router.go`) и `/cabinet/landing`; basename router'а выбирается по `location.pathname` (`resolveBasename()` в `App.tsx`). Бренд и ссылки — из публичного `/auth/bootstrap`, тарифы — из публичного `GET /tariffs`, поддержаны оба `SALES_MODE`. Док: `.cursor/docs/frontend/cabinet-landing.md`, публичный — `documentation/cabinet/landing.md`.
- **Декор-тема `nebula`** — визуальный язык лендинга в кабинете: палитра cyan + фиолетовый, фон со свечением и сеткой, три дрейфующих пятна в сцене, кольцо-градиент и подсветка карточек, лесенка появления, градиентные кнопки, плавное раскрытие `<details>`. Эффекты поверхностей ограничены `.cabinet-shell` (пользовательский кабинет) — в админке `isolation: isolate` запирал бы вложенные поповеры.
- **Стабильные классы-зацепки** для декор-тем: `cabinet-card` (`ui/card.tsx`), `cabinet-btn` / `cabinet-btn-primary` (`ui/button.tsx`), `cabinet-nav-link` и `cabinet-shell` (`AppLayout.tsx`). Без активной темы ничего не стилизуют.
- `useDecorCardSpotlight` — один слушатель `pointermove` на документе пишет `--cd-mx`/`--cd-my`; активен только для `nebula`, выключается при `prefers-reduced-motion` и на тач-устройствах.
- Dev-превью (только `import.meta.env.DEV`, в прод-бандл не попадают): `/cabinet/dev/theme` — декор-темы на заглушках главной и подписки, `/cabinet/dev/connect-cta` — сравнение CTA подключения.

### Changed

- **Кнопка «Подключить устройство»** на дашборде и странице подписки сливалась с карточкой-родителем (заливка совпадала с `--card`). Модификатор `.connect-device-cta--highlight`: акцентная подложка, кольцо по периметру, свечение, стрелка. Селекторы продублированы под декор-темы, у которых правила заливки специфичнее.
- Шаблон nginx в `scripts/meows-shop-setup.sh`: `location /landing` проксируется на бота, а не редиректится на `/cabinet/`.

### Technical

- `internal/cabinet/http/router_spa_test.go` — SPA-хендлер отдаёт `index.html` на `/landing`, `/landing/`, `/cabinet/landing`, а ассеты остаются файлами с `immutable`-кэшем.
- Whitelist `nebula` синхронизирован в трёх местах (`cabinet/config/decor.go`, `config/settings_registry.go`, `decorThemes.ts`) + union `decor_theme` в `lib/api.ts` + i18n `admin.settings.enum.nebula`.

### Changed

- **Remnawave 2.8.x**: `telegramId` в create/update — `int64` (без усечения); `generateUsername` укладывается в лимит API 3–36 символов (fallback `u_`+sha256 при длинных id).

### Added

- **Новые декор-темы кабинета** (`CABINET_DECOR_THEME`): color-only `violet`, `slate`; атмосферные `aurora`, `ocean`, `cyber`, `sunset`, `lavender` (палитра + фон + FX/сцены).
- **Шифрование deep link подключения** (`CABINET_DEEPLINK_HAPP_ENCRYPT`, `CABINET_DEEPLINK_INCY_ENCRYPT`): на странице «Установка» (`/cabinet/connections`) кнопка «Добавить подписку» открывает зашифрованный deep link вместо обычного — `happ://crypt5/` (через официальный API `crypto.happ.su`) и `incy://crypt1/` (обфускация AES-256-GCM, порт `@incy/link-encoder`). Два независимых тумблера, default `false`.
- Админка: **Настройки бота → Оформление кабинета → подсекция «Deep link подключения»** (группа `cabinet`, hot-reload без рестарта).
- API: `GET /cabinet/api/me/deeplink?app=happ|incy` (за `RequireAuth` + `RequireVerifiedEmail`, rate-limit 60 req/min/аккаунт; ответ `{"deeplink":"…"}`, ссылка подписки не логируется). Bootstrap: поля `deeplink_happ_encrypt`, `deeplink_incy_encrypt`.

### Technical

- Пакет `internal/cabinet/deeplink/` (`happ.go`, `incy.go`, `incy_keymat.go`, `util.go`) + known-answer тест совместимости с клиентом INCY (`incy_test.go`); config-getters `internal/cabinet/config/deeplink.go`; whitelist в `settings_registry.go`.
- Док: `.cursor/docs/frontend/cabinet-connection-deeplink.md`.
- **Trade-off Happ:** при `CABINET_DEEPLINK_HAPP_ENCRYPT=true` ссылка подписки уходит на сторонний сервис `crypto.happ.su`. INCY-ключ публичный (зашит в клиенты) — это обфускация от сканеров чатов, не защита секретности.

## [4.11.3] - 2026-06-17

### Added

- **Marketing-режим витрины тарифов** (`CABINET_TARIFF_PRICE_DISPLAY`): `monthly` | `marketing` — эффективная ₽/мес при оплате за 12 месяцев на карточках планов + сноска «*При оплате за год».
- Админка: **Настройки бота → Тарифы** (группа `tariffs`, hot-reload).
- API: `price_display` в `GET /cabinet/api/tariffs`.
- **Подробное описание тарифа** (`tariff.description_detail`, миграция **`000039`**): краткий `description` — витрина и бот; подробный текст — шаг `/cabinet/tariffs?plan=<slug>`. Редактор в web-админке → Тарифы. Toast при сохранении тарифа.

### Changed

- **Web-админка → Настройки бота** (`/admin/settings`): вместо сетки из 13 секций — **5 категорий-вкладок** (Оформление, Продукт, Маркетинг, Операции, Доступ); в категории показываются только её группы-аккордеоны; поиск по всем группам с бейджем категории. Документация для ассистентов: `.cursor/docs/frontend/admin-settings-ui.md`.
- Страница тарифов: цена на карточках — `2.5rem`; синяя сумма на шаге выбора периода — целые рубли без копеек.
- **`TariffDescription`**: `whitespace-pre-wrap` и `remark-breaks` — сохранение ручных пробелов, табов и переносов строк в кратком и подробном описании (превью админки и страница плана).

### Technical

- `internal/cabinet/config/tariffs_display.go`, `web/cabinet/src/features/tariffs/tariffShowcasePrice.ts`.
- Док: `.cursor/docs/frontend/cabinet-tariff-showcase.md`.
- `description_detail`: `tariff.go`, `catalog.go`, `admin_tariffs.go`, `TariffsPage.tsx`, `AdminTariffEditor.tsx`, `TariffDescription.tsx`. Док: `.cursor/docs/frontend/cabinet-tariff-descriptions.md`.
- Зависимость `remark-breaks` в `web/cabinet`.

## [4.11.2] - 2026-06-16

### Added

- **Декор-темы кабинета** (`CABINET_DECOR_THEME`): color-only (`green`, `pink`, `orange`, `yellow`) и сезонные пресеты (`neon`, `new_year`, `summer`, `halloween`, `valentine`, `spring`, `black_friday`).
- Переключение в web-админке: **Настройки → Оформление кабинета** (группа `cabinet`, hot-reload без рестарта).
- Bootstrap: поле `decor_theme` в `GET /cabinet/api/auth/bootstrap`.

### Changed

- Акценты UI кабинета подстраиваются под выбранную декор-тему вместо фиксированного cyan.
- **Профиль:** иконка на кнопке «Привязанные аккаунты».

### Fixed

- CTA «Подключить устройство»: артеfact conic-gradient в decor-темах.
- Spring / Black Friday: старт анимации частиц без задержки при открытии страницы.

### Technical

- `internal/cabinet/config/decor.go`, whitelist в `settings_registry.go`, модуль `web/cabinet/src/features/decor/`.

## [4.11.0] - 2026-06-15

### Added

- **Web-админка кабинета** (`/cabinet/admin/*`, `CABINET_ENABLED=true`, `ADMIN_TELEGRAM_ID`): статистика (в т.ч. колесо фортуны), пользователи и панель Remnawave, промокоды, тарифы, лояльность, **рассылка** (audiences, preview, send через Telegram), infra billing, sync.
- **Admin API** `/cabinet/api/admin/*`: `RequireAuth` + `RequireAdmin`, CSRF на мутациях, rate-limit 120 req/min/аккаунт; feature flags — `GET /admin/bootstrap`.
- **Admin SPA**: `AdminChrome` (отдельный header без user nav), sidebar, breadcrumbs, typed `types/admin.ts`, i18n `admin.*` (ru/en).

### Changed

- **Тёмная тема кабинета**: разведение поверхностей (background → card → header), многослойный фон, elevated cards (`.cabinet-elevated-card`), улучшенный sticky header с blur и cyan-accent border.
- **User cabinet pages**: единые elevated-card поверхности на dashboard, subscription, tariffs, loyalty, profile.

### Technical

- Handlers `internal/cabinet/http/handlers/admin_*.go`, middleware `admin.go`, auth `internal/cabinet/admin/auth/`.
- **Shared broadcast layer** — `internal/broadcast/` (Sender, markup, links); TG-админка и web-админка используют одну логику доставки.
- HTTP-тесты admin middleware и handlers; deep merge i18n для admin keys.

## [4.4.0] - 2026-04-20

### Added

- **Лояльность**: программа XP и уровней скидки (`LOYALTY_ENABLED`), миграции `000014_loyalty`, `000015_loyalty_display_name`, экран пользователя и админка уровней, пересчёт XP из истории покупок.
- **Экран «Мой VPN»**: при достижении месячного лимита трафика показывается пометка (`vpn_traffic_limit_reached`).

### Fixed

- **Продление подписки**: после успешной оплаты подписки вызывается сброс накопленного трафика в панели (`POST .../actions/reset-traffic`), чтобы счётчик не переносился на новый период.

### Technical

- Игнорирование JVM-дампов `hs_err_pid*.log`, `replay_pid*.log`; удалены случайно закоммиченные логи.

## [3.5.0] - 2025-01-XX

### Changed

- **Remnawave API**: Обновлен SDK с `remnawave-api-go v2.2.3` до `v2.3.2`
- **Совместимость**: Версия 3.5.0 совместима с Remnawave начиная от версии 2.3.0+
- **Типы API**: Обновлены типы ответов API:
  - `UserResponseResponse` → `User`
  - `UsersResponseResponseItem` → `User`
  - `GetAllUsersResponseDtoResponseUsersItem` → `User`
  - `GetInternalSquadsResponseDto` → `InternalSquadsResponse`
  - `HwidDevicesResponseResponseDevicesItem` → `Device`
- **Сигнатуры методов API**: Обновлены вызовы методов:
  - `GetAllUsers` теперь принимает `(ctx, float64, float64)` вместо структуры параметров
  - `GetUserByTelegramId` теперь принимает `(ctx, string)` вместо структуры параметров
  - `GetUserHwidDevices` теперь принимает `(ctx, string)` вместо структуры параметров
- **Обработка ошибок**: Убрана обработка `NotFoundError` (больше не возвращается в новой версии API)
- **Обновлены зависимости**:
  - `golang.org/x/text`: `v0.30.0` → `v0.31.0`
  - `github.com/go-faster/jx`: `v1.1.0` → `v1.2.0`
  - `github.com/ogen-go/ogen`: `v1.16.0` → `v1.18.0`
  - `go.uber.org/zap`: `v1.27.0` → `v1.27.1`
  - `golang.org/x/crypto`: `v0.43.0` → `v0.44.0`
  - `golang.org/x/net`: `v0.46.0` → `v0.47.0`
  - `golang.org/x/sync`: `v0.17.0` → `v0.18.0`
  - `golang.org/x/sys`: `v0.37.0` → `v0.38.0`

### Fixed

- **Tribute webhook**: Исправлена паника при обработке успешного платежа для несуществующего клиента
  - Добавлена проверка ошибки после `FindByTelegramId`
  - Добавлена проверка на `nil` для `customer` перед вызовом `CreatePurchase`
- **Поиск пользователей**: Добавлена проверка на пустой ответ при поиске пользователя по Telegram ID в Remnawave API
  - Если API возвращает пустой массив пользователей, создается новый пользователь
- **Доступ к трафику**: Исправлен доступ к использованному трафику через `UserTraffic.UsedTrafficBytes` вместо прямого поля

### Technical

- Обновлены все функции работы с пользователями для использования новых типов API
- Рефакторинг обработки ответов API: переход с `switch` на проверку типа с `type assertion`
- Улучшена обработка пустых ответов во всех методах поиска пользователей
- Обновлены методы `GetUserInfo`, `GetUserTrafficInfo`, `GetUserDevicesByUuid` для новой версии API

### Migration Notes

- ⚠️ **Важно**: При обновлении до версии 3.5.0 убедитесь, что ваш Remnawave сервер обновлен до версии 2.3.0 или выше
- Версия 3.5.0 не совместима с Remnawave версий ниже 2.3.0
- Все уникальные функции бота (стратегии сброса трафика, trial пользователи) сохранены и работают корректно

## [3.4.0] - 2025-11-24

### Added

- **Управление версиями через ldflags**: Добавлены переменные `Version`, `Commit`, `BuildDate` для отслеживания версий приложения
- **Метаданные версии в healthcheck**: Информация о версии доступна в ответе endpoint `/healthcheck`
- **EXTERNAL_SQUAD_UUID**: Параметр конфигурации для назначения внешнего отряда при создании и обновлении пользователей
- **Конфигурация Trial Squads**: Добавлены переменные `TRIAL_INTERNAL_SQUADS` и `TRIAL_EXTERNAL_SQUAD_UUID` для изоляции пробных пользователей в отдельных отрядах
- **Trial Remnawave Tag**: Добавлена переменная `TRIAL_REMNAWAVE_TAG` для назначения отдельного тега пробным пользователям
- **Унифицированные заголовки Remnawave**: Заменен `X_API_KEY` на `REMNAWAVE_HEADERS` с поддержкой множественных заголовков (формат: `key1:value1;key2:value2`)
- **Стратегия сброса трафика**: Добавлены переменные `TRAFFIC_LIMIT_RESET_STRATEGY` и `TRIAL_TRAFFIC_LIMIT_RESET_STRATEGY` для настройки сброса трафика (day/week/month/never)
- **Telegram Stars Payment Gatekeeping**: Добавлена переменная `REQUIRE_PAID_PURCHASE_FOR_STARS` для ограничения доступа к оплате через Telegram Stars только для пользователей с успешными платежами
- **TestHook для Tribute webhook**: Добавлена константа и обработчик для тестирования webhook endpoint без активации реальных событий

### Changed

- **Терминология**: Изменена с "входящего" на "отряд" в настройках и интеграции API
- **Версия Go**: Обновлена с 1.24 до 1.25.3
- **Remnawave API**: Миграция на `remnawave-api-go v2.2.3` с улучшенной поддержкой пагинации
- **Поддержка версий Remnawave**: С версии 3.4.0 бот поддерживает только Remnawave 2.2.\*
- При оплате платной подписки пользователи автоматически переводятся из trial squads в обычные squads
- Обновление пользователей теперь включает изменение внутренних и внешних squads в зависимости от типа подписки
- Все методы создания и обновления пользователей используют соответствующие стратегии сброса трафика (trial или платная)
- Система сборки улучшена за счет явного захвата хеша коммита git в сборках Docker
- Переменные среды `.env.sample` обновлены с добавлением новых параметров конфигурации и документации

### Fixed

- Исправлена ошибка валидации тегов: теги проверяются на соответствие формату `^[A-Z0-9_]+$` перед установкой
- Исправлено отсутствие обновления squads при переходе пользователя с trial на платную подписку
- Сохранение поля языка пользователя во время обновления патча синхронизации
- Улучшена фильтрация проверки имени пользователя для уменьшения ложных срабатываний при сохранении безопасности

### Documentation

- Добавлена подробная документация по `EXTERNAL_SQUAD_UUID` параметрам конфигурации в README
- Обновлен файл README с новыми скриптами сборки и информацией об управлении версиями
- Добавлено описание изменений терминологии в отрядах
- Обновлена таблица совместимости версий Remnawave и бота

### Technical

- Добавлены методы `TrialInternalSquads()`, `TrialExternalSquadUUID()`, `TrialRemnawaveTag()` в `internal/config/cofig.go`
- Добавлен метод `TrafficLimitResetStrategy()` для обычных пользователей
- Обновлены методы `updateUserWithStrategy()` и `createUserWithStrategy()` для поддержки trial конфигурации
- Добавлена функция `isValidTag()` для валидации тегов Remnawave
- Рефакторинг `headerTransport` для поддержки множественных заголовков через `REMNAWAVE_HEADERS`
- Поддержка помощника пагинации через `remnawave-api-go v2.2.3`
- Разработка скрипта сборки Docker (`build-dev.sh`) для упрощения создания локального образа

### Removed

- `X_API_KEY` переменная окружения (заменена на `REMNAWAVE_HEADERS`)

## [3.3.0] - 2025-11-21

### Added

- **Рассылка сообщений для юзеров**: Админ пишет сообщение, отправляет его боту - бот рассылает всем клиентам. 29 сообщений в 1 секунду в порядке очереди. Ограничение телеграм апи - 30 сообщений в 1 сек.
- **Черный список пользователей**: Добавлена переменная окружения `BLOCKED_TELEGRAM_IDS` для блокировки доступа к боту по Telegram ID
- **Белый список пользователей**: Добавлена переменная окружения `WHITELISTED_TELEGRAM_IDS` для обхода проверок на подозрительных пользователей
- **Улучшенная фильтрация подозрительных пользователей**: Теперь проверяются комбинации опасных ключевых слов вместо отдельных слов

### Changed

- Улучшена детекция подозрительных пользователей: проверка комбинаций опасных ключевых слов (telegram+support, telegram+admin, service+support, system+admin, security+admin)
- Легитимные аккаунты проектов (например, @CompanySupportAdmin) теперь проходят проверку
- Сохранена детекция реальных фишинговых аккаунтов (например, @TelegramSupport, @ServiceSupport)

### Fixed

- Исправлены ложные срабатывания для аккаунтов проектов с сервисными именами

### Technical

- Добавлены методы `GetBlockedTelegramIds()` и `GetWhitelistedTelegramIds()` в `internal/config/cofig.go`
- Обновлен `internal/handler/middleware.go` с проверками черного и белого списков
- Добавлены тесты для валидных и подозрительных аккаунтов

## [3.2.9] - 2024-11-08

### Added

- Добавлена новая переменная в `.env` `TRIAL_TRAFFIC_LIMIT_RESET_STRATEGY=day` для управления сбросом трафика триал юзеров (day/week/month/never)
- Для триал юзеров в "подключиться" добавлена информация о лимите трафика подписки

### Changed

- Для платных подписок сброс трафика каждый месяц
- Обновлены переводы

## [3.2.8] - 2024-10-28

### Added

- Добавлена текстовая ссылка на странице "подключиться" для web.telegram юзеров (у них не работает WebApp переход)

## [3.2.7] - 2024-10-28

### Fixed

- Исправлено: кнопка "подключиться" отсутствовала у клиентов с истекшей подпиской

## [3.2.6] - 2024-10-27

### Fixed

- Исправлен баг с `"deviceModel": null` в `devices.go` - бот мог ловить ошибки, когда в HWID прилетает `"deviceModel": null`

### Added

- Добавлена переменная окружения `HEALTH_CHECK_PORT` в `.env` (по умолчанию: 3) для проверки состояния бота и БД

## [3.2.5] - 2024-10-27

### Added

- Добавлены переводы для сообщения заблокированных юзеров с подозрительным ником (можно отредактировать в `en.json` и `ru.json`)

## [3.2.4] - 2024-10-26

### Fixed

- Исправлены баги по отслеживанию команд и сообщений юзеров
- Протестирована обратная совместимость с версиями Remnawave 2.0.0 - 2.0.8

### Changed

- Расширены возможности статистики реферальной системы:
  - **Приглашено**: общее количество рефералов
  - **Оплатили по вашей ссылке**: количество оплативших
  - **Дней заработано**: расчет из базы данных в реальном времени
  - Все данные сохраняются после перезапуска Docker

## [3.2.3 Enhanced] - 2024

### ✨ Новые возможности

#### 📱 Управление устройствами (My Devices)

- Добавлена кнопка "📱 Мои устройства" в меню подключения
- Просмотр списка всех HWID устройств в подписке
- Удаление устройств по клику
- Показ информации: номер, ID устройства, дата добавления
- Конфигурируемый лимит устройств через `HWID_FALLBACK_DEVICE_LIMIT`

#### 🎯 Расширенная реферальная статистика

- **Приглашено**: X — общее количество рефералов
- **Оплатили по вашей ссылке**: X — количество оплативших
- **Дней заработано**: X — расчет из базы данных в реальном времени
- Данные сохраняются после перезапуска Docker

#### 👨‍💼 Отслеживание пользователей для админа

- Глобальное отслеживание ВСЕХ команд (`/start`, `/panel`, `/admin` и т.д.)
- Пересылка всех сообщений админу без блокировки основного функционала
- Система ответов админа через Reply на пересланные сообщения

#### 🆘 Расширенная секция "Помощь"

- Новая кнопка "❓ Помощь" в главном меню
- Подменю с кнопками:
  - "🌏 Какой сервер выбрать"
  - "🆘 Поддержка"
  - "📄 Публичная оферта"
  - "⬅️ Назад" (возврат в главное меню)

### 🐛 Исправления

- **Исправлен баг**: команда `/start` теперь корректно отслеживается админом
- **Исправлен порядок регистрации обработчиков**: устранены конфликты между основной логикой и отслеживанием
- **Обновлены переводы**: корректно работают все 3 параметра в реферальной статистике

### 📋 Технические изменения

- Добавлен файл `internal/handler/devices.go` для управления устройствами
- Обновлен `internal/handler/referral.go` с расширенной статистикой
- Добавлены методы API: `GetUserInfo`, `GetUserDevicesByUuid`, `DeleteUserDevice`
- Добавлены методы БД: `CountPaidReferralsByReferrer`, `CalculateEarnedDays`
- Новые конфигурационные параметры: `HWID_FALLBACK_DEVICE_LIMIT`, `REFERRAL_DAYS`

### ⚙️ Обновленная главная клавиатура

```
[🔥 Попробовать бесплатно] (если новый пользователь)
[💰 Купить]
[🔌 Подключиться]
[🤝 Рефералы] [📊 Статус серверов]
[💬 Отзывы] [📢 Канал]
[❓ Помощь]
```

### 📚 Версии

- **Bot API**: 3.2.3
- **Remnawave API**: 2.1.19

### ⚠️ Важные примечания

- При обновлении проверьте переменную `REFERRAL_DAYS` в `.env` файле
- Значение по умолчанию для `REFERRAL_DAYS`: 15 дней
- При необходимости обновите `HWID_FALLBACK_DEVICE_LIMIT` в конфигурации

### 🔄 Измененные файлы

- `internal/handler/devices.go` (новый)
- `internal/handler/referral.go` (расширено)
- `internal/handler/start.go` (новое меню)
- `internal/handler/connect.go` (кнопка "Мои устройства")
- `internal/handler/handler.go` (отслеживание)
- `internal/remnawave/client.go` (API методы)
- `internal/database/referal.go` (статистика)
- `internal/config/cofig.go` (конфигурация)
- `cmd/app/main.go` (регистрация обработчиков)
- `translations/ru.json` и `translations/en.json` (переводы)
