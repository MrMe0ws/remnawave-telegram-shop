/**
 * Поверхность вложенного элемента внутри карточки партнёрского раздела.
 *
 * Карточка покрашена в --card, и списки, панели и поля ввода с одной только
 * рамкой на ней сливались в общее полотно. Токены поверхностей кабинета
 * разведены по светлоте (фон → muted → card → secondary → border), поэтому шаг
 * «на один вверх» от card — это secondary, и он работает в обеих темах без
 * отдельных правил под каждую.
 *
 * Тот же приём в админской карточке партнёра (AdminPartnerModal, SURFACE):
 * константы разные, потому что модули не связаны, а правило одно.
 */
export const PARTNER_SURFACE = 'border border-border bg-secondary'
