/**
 * Ключ кэша состояния партнёра.
 *
 * Вынесен из PartnerProgramPage отдельным модулем: страница импортирует
 * модалку заявки, а той нужен тот же ключ, чтобы обновить состояние после
 * отправки. Оставь константу на странице — получился бы цикл импортов.
 */
export const PARTNER_STATE_KEY = ['partner-state']
